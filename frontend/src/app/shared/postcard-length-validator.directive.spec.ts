import { PostcardLengthValidatorDirective } from './postcard-length-validator.directive';

describe('PostcardLengthValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PostcardLengthValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
