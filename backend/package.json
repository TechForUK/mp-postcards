{
  "name": "gbhackoff-postcards-actions",
  "version": "1.0.0",
  "description": "OpenWhisk actions for GBHackOff MP Postcards prototype",
  "main": "index.js",
  "scripts": {
    "build": "webpack --config webpack.config.js --progress --colors",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "devDependencies": {
    "webpack": "^4.20.2",
    "webpack-cli": "^3.1.2"
  },
  "dependencies": {
    "@sendgrid/mail": "^6.3.1"
  }
}
